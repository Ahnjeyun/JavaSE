package study01;
/*package test;
import java.util.Scanner;               // Scanner는 util 패키지 안에 들어있다

public class ArithInt {
	public static void main(String[] args) {
		Scanner stdIn = new Scanner(System.in);
		
		System.out.println("x와 y의 사칙연산을 합니다.");
		
		System.out.print("x의 값 : ");   // x 값의 입력을 요청
	int x = stdIn.nextInt();            // x에 정숫값을 입력      
	
	    System.out.print("y의 값 : ");   // y 값의 입력을 요청
	int y = stdIn.nextInt();            // y에 정숫값을 입력
	   
	    System.out.println("x + y = " + (x + y));   // x + y의 값을 표시
	    System.out.println("x - y = " + (x - y));   // x - y의 값을 표시
	    System.out.println("x * y = " + (x * y));   // x * y의 값을 표시
        System.out.println("x / y = " + (x / y));   // x / y의 값을 표시 (몫)
        System.out.println("x % y = " + (x % y));   // x % y의 값을 표시 (나머지)
        
        // stdIn.nextInt()는 키보드에서 입력된 '값'이 된다.
        // main 메소드 중간에서 변수가 선언된 점 -> 메소드 중간이라도 필요한 곳에서 변수를 선언하는 것이 원칙이다.
        // 변수는 필요하게 된 시점에서 선언한다.
        // 변수에 형태에 따라서 next.Int(); 는 정수형 next.Double();은 실수형으로 변함.
	}

}*/
